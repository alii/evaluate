{
  "name": "scraggy",
  "version": "1.0.0",
  "type": "module",
  "description": "A safe JavaScript expression evaluator",
  "license": "MIT",
  "scripts": {
    "test": "bun test",
    "build": "bun build ./src/index.ts --outdir ./dist --target node --external bun --external acorn"
  },
  "dependencies": {
    "acorn": "^8.11.3"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.3.3"
  }
}
